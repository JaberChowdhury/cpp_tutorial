#pragma once
#include <string>
#include <vector>

namespace color {

/// Generate a vector of color names up to `max` elements.
std::vector<std::string> gencolor(unsigned int max);

} // namespace color
