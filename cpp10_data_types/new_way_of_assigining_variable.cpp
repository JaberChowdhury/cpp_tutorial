#include <iostream>
using namespace std;
int main()
{
    // bool isAlive{false};

    // // Error! This brace syntax is only for initialization
    // isAlive{true};
    bool isAlive{false};

    isAlive = true;
    cout << isAlive << endl;
    return 0;
}