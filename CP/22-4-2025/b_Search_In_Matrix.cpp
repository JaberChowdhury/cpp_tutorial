#include <iostream>
#include <vector>
using namespace std;

int main() {
    int m, n, target;
    cin >> m >> n;
    vector<vector<int>> data(10);
    for (int i = 0; i < m; i++) {
        for (int k = 0; k < n; k++) {
            int h = 0;
            cin >> h;
        }
    }

    return 0;
}

// returning soon
